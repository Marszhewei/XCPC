#include <bits/stdc++.h>

int main(void)    {
    system("data > test.in");
    system("demo1 < test.in > a.out");
    system("demo2 < test.in > b.out");
    if (system("fc a.out b.out"))    return 0;
    return 0;
}