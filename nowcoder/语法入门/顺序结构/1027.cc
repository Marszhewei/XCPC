#include <iostream>

void solve()
{
    int a, b;
    std::cin >> a >> b;
    std::cout << (a+b)*2 << std::endl << (a*b) << std::endl;
}

int main(int argc, char **argv)
{
    solve();
    return 0;
}